import { SortTripsPipe } from './sort-trips.pipe';

describe('SortTripsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTripsPipe();
    expect(pipe).toBeTruthy();
  });
});
