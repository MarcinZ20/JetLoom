import { FilterTripsByTopBarPipe } from './filter-trips-by-top-bar.pipe';

describe('FilterTripsByTopBarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTripsByTopBarPipe();
    expect(pipe).toBeTruthy();
  });
});
